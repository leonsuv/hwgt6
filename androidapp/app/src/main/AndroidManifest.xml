<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Zugriff auf die Opel-Schnittstelle (HTTPS) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Der Server fuer die Uhr muss dauerhaft lauschen -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Akku-Ausnahme: sonst kein Netz im Hintergrund fuer den WatchProvider -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Wear Engine laeuft ueber HMS Core und Huawei Health -->
    <queries>
        <package android:name="com.huawei.health" />
        <package android:name="com.huawei.hwid" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/ic_menu_compass"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:label="@string/login" />

        <service
            android:name=".BridgeService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Datenquelle fuer Gadgetbridge; Android startet die App dafuer bei
             Bedarf selbst, auch nachdem der Akku-Manager sie beendet hat. -->
        <provider
            android:name=".WatchProvider"
            android:authorities="de.saigak.opelbridge.watch"
            android:exported="true" />

        <!-- AGC-App-ID, unter der Wear Engine freigeschaltet ist -->
        <meta-data
            android:name="com.huawei.hms.client.appid"
            android:value="${agcAppId}" />

    </application>
</manifest>
